plan_task:
  description: >
    Read the task: {task}

    Produce a markdown implementation plan for McKenna Derby with these sections:
    - Context (repo paths, why now, constraints)
    - Goal (checkboxes)
    - Non-goals (checkboxes)
    - Assumptions
    - Numbered tasks with files, exact changes, and verify commands
    - Test plan
    - Rollout / notes

    Ground rules:
    - Main package: mckenna_derby/; venv at repo root .venv (Python 3.9)
    - FROZEN unless task says otherwise: timewave.py, novelty.py, compare.py
    - prereg.json must not be edited after first real-data run
    - After each task touching code: .venv/bin/pytest -q
    - Prefer minimal diffs; no dashboards/APIs unless requested

    Write the full plan as your final answer.
  expected_output: >
    A complete markdown plan suitable for .cursor/plans/PLAN.md, with copy-pasteable
    verify commands and file paths under /Users/thedao/Projects/mckenna-derby.
  agent: planner
